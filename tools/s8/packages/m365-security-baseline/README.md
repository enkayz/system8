# M365 Security Baseline

Evidence-led, read-only checks for Conditional Access, privileged roles, application publisher posture and credential expiry.

```powershell
s8 install m365-security-baseline
s8secure full -InstallDependencies
s8secure access
```

Findings are advisory and mapped to explicit collected evidence. No remediation is performed.
